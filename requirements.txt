yfinance
pandas
ta
matplotlib
scikit-learn
lxml
numpy
pytest
xlrd>=2.0.1  # read the GPR .xls (geopolitical-risk index) in modules/geocontext.py
streamlit    # lens_web.py — the Lens as a local web page (S48)
ansi2html    # lens_web.py + api/ — terminal-color report → HTML
plotly       # lens_web.py + api/charts.py — interactive candlestick (supersedes sixel in the browser)
fastapi      # api/ — React-frontend backend (S60)
uvicorn[standard]  # api/ — ASGI server
cachetools   # api/cache.py — TTL caches replacing st.cache_data